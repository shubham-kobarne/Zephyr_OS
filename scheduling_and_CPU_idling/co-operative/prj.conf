# Enable basic threading
CONFIG_MAIN_STACK_SIZE=1024
CONFIG_THREAD_NAME=y

# Use simple scheduler (optional, works with others too)
CONFIG_SCHED_SIMPLE=y

# Enable printk for console output
CONFIG_PRINTK=y
